package laststats_test

// todo
/*func TestParseResponse(t *testing.T) {
	t.Run("error", func(t *testing.T) {
		result, err := lastscores.ParseResponse([]byte{}, errors.New("some error"))
		assert.Equal(t, []string{}, result)
		assert.ErrorContains(t, err, "some error")
	})

	t.Run("no error", func(t *testing.T) {
		/*t.Run("invalid response body", func(t *testing.T) {
			t.Run("empty", func(t *testing.T) {
				result, err := lastscores.ParseResponse([]byte("\n"), nil)
				assert.Equal(t, []string{}, result)
				assert.ErrorContains(t, err, "invalid response body")
			})

			t.Run("not containing expected response", func(t *testing.T) {
				result, err := lastscores.ParseResponse([]byte("fooooo"), nil)
				assert.Equal(t, []string{}, result)
				assert.ErrorContains(t, err, "invalid response body")
			})
		})

		t.Run("valid response body", func(t *testing.T) {
			scores, err := mvdsv.GetLastStats("quake.se:28501")
			fmt.Println("scores", scores)
			fmt.Println("err", err)
			t.Fail()
		})
	})
}*/
